name: Cleanup PR Preview

on:
  pull_request:
    types:
      - closed  # Kjører når PR lukkes (merge eller close)

jobs:
  cleanup:
    name: Delete PR Preview
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'
      id-token: 'write'
      pull-requests: write
    steps:
      - uses: actions/checkout@v5

      - name: Login to NAIS
        uses: nais/login@v0
        with:
          team: teamdagpenger
          identity_provider: ${{ secrets.NAIS_WORKLOAD_IDENTITY_PROVIDER }}
          project_id: ${{ vars.NAIS_MANAGEMENT_PROJECT_ID }}

      - name: Set up kubectl credentials
        run: |
          gcloud container clusters get-credentials dev-gcp --region europe-north1 --project ${{ vars.NAIS_MANAGEMENT_PROJECT_ID }}

      - name: Delete NAIS application
        uses: nais/deploy/actions/delete@v2
        env:
          TEAM: teamdagpenger
          CLUSTER: dev-gcp
          RESOURCE: dp-rapportering-saksbehandling-frontend-pr-${{ github.event.pull_request.number }}
          IDENTITY_PROVIDER: ${{ secrets.NAIS_WORKLOAD_IDENTITY_PROVIDER }}
          PROJECT_ID: ${{ vars.NAIS_MANAGEMENT_PROJECT_ID }}

      - name: Comment on PR
        if: always()
        uses: actions/github-script@v8
        with:
          script: |
            const prNumber = context.payload.pull_request.number;
            const appName = `dp-rapportering-saksbehandling-frontend-pr-${prNumber}`;
            github.rest.issues.createComment({
              issue_number: prNumber,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `🧹 **Preview cleanup**\n\nPR preview app \`${appName}\` har blitt slettet fra NAIS.`
            })
